# Garlic-OS
毕业设计，尝试自己实现一个OS
这里记录一些知识点

# 磁盘的启动扇区
电脑启动时会跳转到启动盘的第一个扇区，该扇区最后两字节必须是以魔数“0xAA55”结尾

# QEMU
QEMU是一个纯软件实现的电脑，用软件来实现CPU,内存等实体

# 汇编
“times 510-($-$$) db 0”
$：代表当前指令的地址。
$$：代表程序的起始地址。
$-$$：计算出从开始到当前位置一共占用了多少字节
这是一个编译器指令，意思是“重复执行”。它会将剩余的空间全部填充为0，直到达到第510字节

“dw”
定义一个字（Define Word），占用 2 字节
